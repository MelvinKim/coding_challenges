package com.company.strings.medium;

public class LongestSubstringWithAtMostKDistinctLetters {
    public int solution(String s ) {

        //TODO: Remember to change
        return -1;
    }
}
/*
- if it's empty ?? || k > n, -> return 0;
- unique: Set, max = 0,
- Two loops
    for(i =0; i < n; i++) {
        for(int j=0; j < n; j++) {
            if(!set.contains(s.charAt(j) && set.size() <k) {
                set.add(s.charAt(j));
                max = Math.max(max, set.size());
            } else {
                max = 
            }
        }
    }

 */